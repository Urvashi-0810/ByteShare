<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ByteShare" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/bg_cream</item>
        <item name="colorPrimary">@color/primary_black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_lime</item>
        <item name="colorOnSecondary">@color/primary_black</item>
        <item name="android:statusBarColor">@color/bg_cream</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.ByteShare" parent="Base.Theme.ByteShare" />
</resources>